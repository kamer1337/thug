; This file contains interface definitions for script keys

Class KeyFade
{
	Mode = In
	Duration = 100

	// @nextparm | Mode | list | In | Out | done
	// @nextparm | Duration | slidernum | 0 | 100 | 1
	// @nodecolor | [128,128,128]

	// @template | print   | // My script to do Fade<Mode>
	// @template | printif | Mode | In | FadeIn Dur=<Duration>
	// @template | printif | Mode | Out | FadeOut Dur=<Duration>
}

; This class is internally defined  DO NOT REMOVE
Class KeyObj
{
	Type Enable
	{
		Node = [none]
	
		// @nextparm | Node | node
		// @nextdesc | Node | Determines the node that should be enabled at this time
	}

	Type Disable
	{
		Node = [none]
		
		// @nextparm | Node | node
		// @nextdesc | Node | Determines the node that should be disabled at this time
	}
}
